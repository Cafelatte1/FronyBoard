import "@testing-library/jest-dom/vitest";

// jsdom has no matchMedia — stub it as "desktop" (matches: false) so useIsPhone works.
Object.defineProperty(window, "matchMedia", {
  writable: true,
  value: (query: string) => ({
    matches: false,
    media: query,
    onchange: null,
    addEventListener: () => {},
    removeEventListener: () => {},
    addListener: () => {},
    removeListener: () => {},
    dispatchEvent: () => false,
  }),
});
